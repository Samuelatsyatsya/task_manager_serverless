version: 1
frontend:
  appRoot: frontend
  phases:
    preBuild:
      commands:
        - nvm install 18
        - nvm use 18
        - npm ci --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*